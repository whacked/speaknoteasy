(ns speaknoteasy.core
  (:use
   clojure.test
   clojure.pprint ; Amotoen doesn't require this, just the sample code below
   ;; com.lithinos.amotoen.grammars.csv


   ;; speaknoteasy.grammar.org
   ))

;; (deftest use-amotoen
;;   (pprint (to-clj "a,b,c")))

